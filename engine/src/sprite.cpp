// #include "sprite.h"

// Sprite::Sprite(std::shared_ptr<Texture> tex, glm::ivec2 size, bool useAtlas, glm::ivec2 atlasCoords, SpriteSheet sSheet) {
//     m_texture = tex;
//     m_size = size;
//     m_atlasCoords = atlasCoords;
//     usingAtlas = useAtlas;

//     index = (usingAtlas) 
//             ? sSheet.getSpriteIndex(atlasCoords)
//             : tex->getTextureID();
// }

// Sprite::~Sprite() {

// }